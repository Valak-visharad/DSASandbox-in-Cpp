#include<iostream>
using namespace std;

int main()
{
    int arr[100];
    // int elem[100];
    // int pos[100];
    int len;
    int num;
    cin >> len;
    for(int i = 1; i <= len; i++)
    {
        cin >> arr[i];
    }

    cin >> num;

    for(int i=0;i<num;i++)
    {
        
    }
    // int pos[num];

    // for(int i = 0; i < num; i++)
    // {
    //     cin >> pos[i];
    // }

    // for(int i = 0; i < num; i++)
    // {
    //     int position = pos[i];
        
    // }

}